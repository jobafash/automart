import React from "react";
import { Helmet } from "react-helmet";

const Meta = ({ title, description, keywords }) => {
  return (
    <Helmet>
      <title>{title}</title>
      <meta name="description" content={description} />
      <meta name="keyword" content={keywords} />
    </Helmet>
  );
};

Meta.defaultProps = {
  title: "Welcome To Auto Mart",
  description: "We sell the best automobiles for cheap",
  keywords: "automobiles, buy cars, cheap cars",
};

export default Meta;
